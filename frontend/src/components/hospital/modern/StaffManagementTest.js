import React from 'react';
import { Box, Typography } from '@mui/material';

const StaffManagementTest = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Staff Management
      </Typography>
      <Typography variant="body1">
        Staff management functionality is temporarily unavailable. Please check back later.
      </Typography>
    </Box>
  );
};

export default StaffManagementTest;